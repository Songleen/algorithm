希尔排序：普通的插入排序在插入的数据较小的时候，元素移动的次数会明显增多，
        希尔排序就是为了解决这样的问题。

希尔排序是插入排序的一种，也叫最小增量排序。

文档中提到，希尔排序里面的第二个循环用到了直接插入排序，在这里数组中的10个数据被分成了2组，
每组5个，遍历交换数据的时候，并不是每组遍历完了之后，再遍历另一组，而是第一次去第一组的前两个
进行比较，第二次去第二组前两个进行比较，第三次再去第一组二三个进行比较…………如此轮替进行，这点很重要。

所以到最后一次数组的步长变为1的时候，其实是挨着挨着走了一遍，将数组的元素规则排序。