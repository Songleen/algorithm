基数排序，代码实现过程中，需要注意的有：
      1、用一个二维数组来表示桶中桶的关系；
      2、用一个临时数组来表示内桶中存放元素的个数，在遍历取出的时候需要用到；
      3、每一次内桶的元素被取完之后，需要将临时数组重新置零，否则会出现指针溢出；
      4、for (int l=0,n = 1; l < maxLength; l++,n *= 10)，最外层的循环格式固定，
         其中的l是起辅助作用，最中目的是用到n;