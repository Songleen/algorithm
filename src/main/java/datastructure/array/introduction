数组扩容：需要在底层创建新的数组，将原来的数组拷贝到新数组，并插入新的数据；

将原始二维数组转成稀疏数组的难点：稀疏数组只有3列，它的第一行三个值是确定的，
                            其余行的行号，是根据要存放的值是原始数组的第几个非零数组确定。

将稀疏数组还原成二维数组：其实很简单，稀疏数组的第二维三个值是可以写死的。
                             for (int i = 1; i < sparseArr.length; i++) {
                                         originalArr[sparseArr[i][0]][sparseArr[i][1]] = sparseArr[i][2];
                                     }